运行说明：
（1）实验结果被封装在了GizmoImpl.jar中。由于exe文件的特殊性不能上传，如果需要运行，请打开命令行，并重定位到GizmoImpl.jar所在文件夹。输入java -jar GizmoImpl.jar即可运行。
（2）F键可以使挡板翻转。
（3）电脑需要已经配置Java环境（在命令行中输入java -version可以返回版本号即可）。
（4）如果不能正常运行，实验报告中有屏幕截图，供参考。
